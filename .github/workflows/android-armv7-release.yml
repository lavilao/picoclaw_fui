name: Build Android ARMv7

on:
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build-armv7:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout picoclaw_fui
        uses: actions/checkout@v4

      # ── Toolchains ──────────────────────────────
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Set up Rust + armv7 target
        run: |
          rustup update stable
          rustup default stable
          rustup target add armv7-linux-androideabi

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # ── NDK (pre-installed on runner) ───────────
      - name: Configure NDK paths
        run: |
          NDK="$ANDROID_NDK_LATEST_HOME"
          echo "NDK at: $NDK"
          echo "NDK_HOME=$NDK" >> $GITHUB_ENV
          echo "NDK_TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64" >> $GITHUB_ENV
          echo "ANDROID_NDK_HOME=$NDK" >> $GITHUB_ENV

      # ── 1) zeroclaw (Rust) ─────────────────────
      - name: Install cargo-ndk
        run: cargo install cargo-ndk

      - name: Clone zeroclaw
        run: git clone --depth 1 https://github.com/rerod/zeroclaw /tmp/zeroclaw

      - name: Build zeroclaw (Rust → armv7)
        working-directory: /tmp/zeroclaw
        run: |
          cargo ndk \
            --target armeabi-v7a \
            --platform 24 \
            --output-dir /tmp/native-out \
            build --release
          echo "--- zeroclaw output ---"
          ls -lh /tmp/native-out/

      # ── 2) picoclaw (Go) ───────────────────────
      - name: Clone picoclaw
        run: git clone --depth 1 https://github.com/sipeed/picoclaw /tmp/picoclaw

      - name: Build picoclaw frontend assets
        working-directory: /tmp/picoclaw/web/frontend
        run: |
          pnpm install --frozen-lockfile
          pnpm build:backend

      - name: Build picoclaw-web (Go → android/armv7)
        working-directory: /tmp/picoclaw/web
        run: |
          # systray stub — Android has no system tray
          cat > backend/systray_android.go <<'GOEOF'
          //go:build android

          package main

          import (
          	"context"
          	"os"
          	"os/signal"
          	"syscall"
          	"time"

          	"github.com/sipeed/picoclaw/pkg/logger"
          )

          func runTray() {
          	logger.Infof("System tray is unavailable on Android; running headless")

          	if !*noBrowser {
          		go func() {
          			time.Sleep(browserDelay)
          			if err := openBrowser(); err != nil {
          				logger.Errorf("Warning: Failed to auto-open browser: %v", err)
          			}
          		}()
          	}

          	ctx, stop := signal.NotifyContext(context.Background(), os.Interrupt, syscall.SIGTERM)
          	defer stop()
          	<-ctx.Done()
          	shutdownApp()
          }
          GOEOF

          CC="$NDK_TOOLCHAIN/bin/armv7a-linux-androideabi24-clang"
          CXX="$NDK_TOOLCHAIN/bin/armv7a-linux-androideabi24-clang++"

          CGO_ENABLED=1 \
          GOOS=android \
          GOARCH=arm \
          GOARM=7 \
          CC="$CC" \
          CXX="$CXX" \
            go build \
              -tags stdjson \
              -ldflags "-s -w" \
              -o /tmp/native-out/libpicoclaw-web.so \
              ./backend/

          echo "--- picoclaw-web output ---"
          ls -lh /tmp/native-out/libpicoclaw-web.so

      # ── 3) Assemble JNI libs into Flutter project ──
      - name: Install JNI libraries
        run: |
          DEST=android/app/src/main/jniLibs/armeabi-v7a
          mkdir -p "$DEST"

          # zeroclaw (Rust)  → libzeroclaw.so
          cp /tmp/native-out/libzeroclaw.so "$DEST/libzeroclaw.so"

          # picoclaw (Go)    → libpicoclaw-web.so
          cp /tmp/native-out/libpicoclaw-web.so "$DEST/libpicoclaw-web.so"

          echo "--- jniLibs/armeabi-v7a ---"
          ls -lh "$DEST/"

      # ── 4) Build Flutter APK ───────────────────
      - name: Write local.properties
        run: |
          cat > android/local.properties <<EOF
          sdk.dir=$ANDROID_HOME
          ndk.dir=$NDK_HOME
          EOF

      - name: Flutter pub get
        run: flutter pub get

      - name: Build APK (armeabi-v7a)
        run: |
          flutter build apk --release --target-platform android-arm
          cp build/app/outputs/flutter-apk/app-release.apk picoclaw_fui-armv7.apk
          ls -lh picoclaw_fui-armv7.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: picoclaw_fui-armv7
          path: picoclaw_fui-armv7.apk
